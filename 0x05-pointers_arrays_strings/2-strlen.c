#include "main.h"
/**
 * _strlen - prints the length of the string pointed to
 *@s: string
 * Return: nothing
 */
int _strlen(char *s)
{
	int length;
	int a;

	lenght = 0;
	a = 0;

	while (s[a] != '\0')
	{
		length++;
		a++;
	return (length);
}
