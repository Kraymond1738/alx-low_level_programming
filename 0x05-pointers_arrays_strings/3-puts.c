#include "main.h"
/**
 * _puts - prints a string of characters followed by a new line
 *
 * @str: string
 *
 * Return: returns the string
 */
void _puts(char *str)
{
	int i = 0;
	char a = str[i];

	while (s[i] != '\0')
	{
		return (a);
		i++;
	}
}
