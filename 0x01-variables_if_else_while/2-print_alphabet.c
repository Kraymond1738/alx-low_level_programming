#include <stdio.h>
/**
 * main - prints the alphabet in lower case
 *
 * Return: always a 0
 */
int main(void)

{
	char ab;

	for (ab = 'a' ; ab <= 'z' ; ab++)
	{
		putchar(ab);
	}
putchar('\n');
return (0);
}
